{
  "name": "asistentecarmen",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "test": "esbuild src/lib/portalHorarios.ts src/lib/horarioOficialStore.ts src/lib/fechasStore.ts src/data/evaluacion.ts --bundle --format=esm --platform=neutral --outdir=.test-build --out-extension:.js=.mjs --log-level=error && node --test src/lib/*.test.mjs src/data/*.test.mjs"
  },
  "dependencies": {
    "@block65/webcrypto-web-push": "^1.0.2",
    "hono": "^4.6.14"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241218.0",
    "typescript": "^5.7.2",
    "wrangler": "^3.99.0"
  }
}